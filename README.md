# getFeautreSamples
Pulls a random sample of N records per group value (e.g. per county, per district, per category) from an ArcGIS hosted feature service, then exports the results as a file geodatabase feature class, a CSV of selected fields, or both.
